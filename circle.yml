machine:
  node:
    version: 8.1.2

dependencies:
  pre:
    - npm install -g firebase-tools

test:
  override:
    - echo "What could go wrong"

deployment:
  production:
    branch: master
    commands:
      - yarn build
      - firebase deploy --token "$FIREBASE_TOKEN"
