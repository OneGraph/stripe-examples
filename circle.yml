machine:
  node:
    version: 8.1.2

dependencies:
  override:
    - npm install -g firebase-tools
    - yarn install

test:
  override:
    - echo "What could go wrong"

deployment:
  production:
    branch: master
    commands:
      - yarn build
      - firebase deploy --token "$FIREBASE_TOKEN"
